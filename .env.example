# User/Group ID mapping (run `id -u` and `id -g` to find yours)
PUID=1000
PGID=1000

# Timezone (e.g., America/Chicago, Europe/London, Asia/Tokyo)
TZ=UTC

# Directory containing your code on the host
CODE_DIR=${HOME}/code

# Name shown in VS Code Remote Tunnels
TUNNEL_NAME=antigravity-server

# SSH Agent socket (for Git authentication)
# Uncomment and set if you want SSH agent forwarding
# SSH_AUTH_SOCK=/run/user/1000/ssh-agent.socket
