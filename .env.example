# Antigravity Server Configuration
# Copy this file to .env and customize as needed.
# These defaults work out-of-the-box for most setups.

# ─────────────────────────────────────────────────────────────
# User Mapping (IMPORTANT for file permissions)
# ─────────────────────────────────────────────────────────────
# Run `id -u` and `id -g` on your host to find your values.
# Default 1000 works for most single-user Linux systems.
PUID=1000
PGID=1000

# ─────────────────────────────────────────────────────────────
# Paths
# ─────────────────────────────────────────────────────────────
# Directory containing your code on the host.
# NOTE: Docker Compose does not expand "~" here; use an absolute path.
# Leave this unset to use the default from docker-compose.yml ($HOME/code).
# CODE_DIR=/home/youruser/code

# ─────────────────────────────────────────────────────────────
# Tunnel Settings
# ─────────────────────────────────────────────────────────────
# Name shown in VS Code Remote Tunnels list
TUNNEL_NAME=antigravity-server

# ─────────────────────────────────────────────────────────────
# Optional Settings
# ─────────────────────────────────────────────────────────────
# Timezone (for correct timestamps in logs)
TZ=UTC

# SSH Agent socket for Git authentication
# Uncomment if you use SSH keys for Git:
# SSH_AUTH_SOCK=/run/user/1000/gnupg/S.gpg-agent.ssh
